/** Login gate (Netlify Identity). Implemented in T1. */
export default function Login() {
  return (
    <section className="page">
      <h1>Login</h1>
      <p className="placeholder">Netlify Identity login is wired up in task T1.</p>
    </section>
  );
}
